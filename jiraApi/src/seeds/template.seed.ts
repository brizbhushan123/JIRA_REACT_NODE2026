// Template seeding is not required for the Jira schema.
// Add Jira-specific seed data here when needed.

const seedTemplateDetails = async () => {
  // no-op
};

export default seedTemplateDetails;
